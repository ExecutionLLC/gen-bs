{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "One view",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "UUID, the unique identifier for the view"
    },
    "view_id": {
      "type": "string",
      "description": "UUID, the unique identifier for the parent view"
    },
    "created": {
      "type": "timestamp"
    },
    "creator": {
      "type": "string",
      "description": "UUID, the unique identifier for the user"
    },
    "type": {
      "type": "string",
      "description": "standard/default - available for demo-user, user - created by user, default - default view for analyses",
      "enum": [
        "standard",
        "default",
        "user"
      ]
    },
    "is_copy_disabled": {
      "type": "boolean",
      "description": "If true, user should not be allowed to copy the view on UI.(not used)"
    },
    "is_deleted": {
      "type": "boolean",
      "description": "(not used)"
    },
    "view_list_items": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "./view_list_item.json#"
      }
    }
  },
  "required": [
    "id",
    "name",
    "type",
    "view_list_items",
    "is_copy_disabled"
  ]
}