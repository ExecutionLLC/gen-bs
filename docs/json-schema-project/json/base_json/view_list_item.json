{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"type": "object",
	"properties": {
		"id": {
			"description": "The unique identifier for a settings row",
			"type": "number"
		},
		"field_name": {
			"type": "string",
			"description": "name of the field in sample."
		},
		"source_name": {
			"type": "string",
			"description": "Null for sample fields, otherwise contains name of the source field is contained in."
		},
		"order": {
			"description": "The order of column in list",
			"type": "integer"
		},
		"sort_order": {
			"description": "The order of column in multi_sort_settings",
			"type": "integer"
		},
		"sort_direction": {
			"description": "Sort direction",
			"type": "string",
			"enum": [
				"asc",
				"desc"
			]
		},
		"keywords": {
			"type": "array",
			"uniqueItems": true,
			"items": {
				"$ref": "./keyword.json#"
			}
		}
	},
	"required": [
		"id",
		"order"
	]
}
