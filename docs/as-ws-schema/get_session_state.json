{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "get_session_state - backend API function",
  "description": "Ask for backend session state",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Session id to which the state request is related."
    },
    "method": {
      "type": "string",
      "enum": [
        "v1.get_session_state"
      ]
    }
  }
}