{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "converts user VCF to HDF5 format and pushes both files into storage. This is a REST request with the following parameters.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Upload session identifier."
    },
    "sample_id": {
      "type": "string",
      "description": "UUID of the sample on WS."
    }
  }
}