{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "converts user VCF to the internal format and pushes both files into storage.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Upload session identifier."
    },
    "sample_id": {
      "type": "string",
      "description": "UUID of the sample on WS."
    }
  }
}