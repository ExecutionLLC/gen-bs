{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "v1.load_file",
  "title": "load_file - backend API function",
  "description": "converts user VCF to HDF5 format and pull both files on storage",
  "type": "object",
  "properties": {
    "params": {
      "type": "object",
      "properties": {
        "sample_id": {
          "type": "string",
          "description": "UUID of the sample on WS."
        },
        "size": {
          "type": "string",
          "description": "Sample file size in bytes"
        }
      }
    },
    "result": {
      "type": "object",
      "properties": {
        "status": {
          "enum": [
            "converting",
            "uploading",
            "ready"
          ]
        },
        "sample_id": {
          "type": "string",
          "description": "UUID of the sample to which the message is related to."
        },
        "progress": {
          "type": "integer",
          "description": "progress of operation in %. Operation correspond to enum value in status."
        },
        "metadata": {
          "$ref": "base_json/file_metadata.json#",
          "description": "Sent one time when the file is parsed."
        }
      }
    }
  }
}